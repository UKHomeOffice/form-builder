apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: {{.FORMBUILDER_NAME}}-pod-tls
spec:
  secretName: {{.FORMBUILDER_NAME}}-pod-tls
  issuerRef:
    name: platform-tls
    kind: ClusterIssuer
  commonName: {{.FORMBUILDER_NAME}}.{{.FORMBUILDER_KUBE_NAMESPACE}}.svc.cluster.local
  dnsNames:
  - localhost
  - 127.0.0.1
